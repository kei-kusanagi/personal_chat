name: personal_messenger
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0-246.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  supabase_flutter: ^1.10.9
  provider: ^6.0.5
  flutter_colorpicker: ^1.0.3
  dio: ^5.3.0
  timeago: ^3.5.0
  image_picker: ^1.0.1
  file_picker: ^5.3.3
  cached_network_image: ^3.2.3
  flutter_file_downloader: ^1.1.2 #  android
  download: ^1.0.0 #  windows and mac

  path_provider: ^2.0.15
  video_thumbnail: ^0.5.3





dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:


  uses-material-design: true

